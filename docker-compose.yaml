version: '3.8'
services:
  character-frontend:
    container_name: character-frontend
    image: ${REPO_IP}/service1-frontend:${app_version}
    build: ./service_1
    deploy:
      replicas: 5
    ports:
    - target: 5000
      published: 5000
      protocol: tcp
    environment:
      DATABASE_URI: ${DATABASE_URI}
  service2-backend:
    container_name: service2-backend
    image: ${REPO_IP}/service2-backend:${app_version}
    build: ./service_2
    deploy:
      replicas: 5
    ports:
    - published: 5001
      target: 5001
  service3-backend:
    container_name: service3-backend
    image: ${REPO_IP}/service3-backend:${app_version}
    build: ./service_3
    deploy:
      replicas: 5
    ports:
    - published: 5002
      target: 5002
  service4-backend:
    container_name: service4-backend
    image: ${REPO_IP}/service4-backend:${app_version}
    build: ./service_4
    deploy:
      replicas: 5
    ports:
    - published: 5003
      target: 5003